package com.example.book.ui.extra;


public class Enums {
    public enum BookCondition {
        NEW,
        USED
    }
    public enum BookCategory {
        ACADEMIC,
        GENERAL
    }

    public enum BidStatus {
        PENDING,
        ACCEPTED,
        REJECTED
    }
    public enum PostType{
        FEATURED,
        NORMAL
    }
}
